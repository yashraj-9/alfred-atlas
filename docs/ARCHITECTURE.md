# Architecture

`core/orchestrator.py` coordinates profile storage, career analysis, roadmap
generation, and recommendations. Data-backed modules stay offline-first so the
starter remains usable before external APIs are connected.
